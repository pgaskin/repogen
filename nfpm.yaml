name: repogen
arch: amd64
platform: linux
version: ${APP_VERSION_NO_V}
section: utils
priority: optional

replaces:
provides:
depends:
recommends:
suggests:
conflicts:

maintainer: Patrick Gaskin <geek1011@outlook.com>
description: |1
  easy-to-use debian repository generator
  repogen is an easy way to generate Debian repositories.
  .
  Features:
  - Zero configuration
  - Override Maintainer field
  - Web interface for browsing packages
  - Fast
  - Generates Contents index
  - Multiple distributions and components
  - GPG signing
  - Automatic updating
vendor: Patrick Gaskin
homepage: https://github.com/geek1011/repogen
license: MIT

bindir: /usr/bin
files:
  ./build/repogen-linux-64bit: /usr/bin/repogen
config_files:
